import SwiftUI

class _asd21: ObservableObject {
    let contact = asd13_()

    func createDartsGameScene(gameData: ga) -> dgso {
        let scene = dgso()
        scene.game  = gameData
        return scene
    }
}
